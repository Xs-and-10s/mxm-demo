// lib/env.ts
// import { type } from "arktype";

// const Env = type({
//   NEXT_PUBLIC_API_URL: "string>url",
//   GRAPHQL_ENDPOINT: "string>url",
//   NODE_ENV: "'development'|'test'|'production'"
// });

// const res = Env(process.env);
// if (res.problems) throw new Error(res.problems.summary);
// export const env = res.data;
